// Hardcoded constants for development (no auth).
// When we add Auth later, these will be replaced by session lookups.

export const DEV_USER_ID = 'dev-user-local-001';
export const DEV_WORKSPACE_ID = 'dev-workspace-local-001';
